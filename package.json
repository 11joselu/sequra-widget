{
  "name": "sequra",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "api": "cd api/ && npm start",
    "predev": "npm run api &",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^6.3.5"
  }
}
